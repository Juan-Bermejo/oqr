import { ScrollbarStyleDirective } from './scrollbar-style.directive';

describe('ScrollbarStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollbarStyleDirective();
    expect(directive).toBeTruthy();
  });
});
