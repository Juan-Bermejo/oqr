import { RemovePipePipe } from './remove-pipe.pipe';

describe('RemovePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RemovePipePipe();
    expect(pipe).toBeTruthy();
  });
});
